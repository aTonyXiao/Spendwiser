const fs = require('fs');
const readline = require('readline');

/**
 * loads a card into firebase given card information 
 * @param {Object} card - an object with the following format:
 * 
 */
exports.load_card = function(card) {

}